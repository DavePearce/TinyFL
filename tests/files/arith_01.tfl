function main() {
    assert 1 == 1;
    assert 1 != 2;
    assert (1+1) == 2;
    assert (2+1) == 3;
    assert (1+2) == 3;
    assert (2-1) == 1;
    assert (1*2) == 2;
    assert (2*3) == 6;
    assert (6/3) == 2;
    assert 1 < 2;
    assert 1 <= 1;
    assert 1 <= 2;
    assert 2 > 1;
    assert 2 >= 1;
    assert 2 >= 2;
}
